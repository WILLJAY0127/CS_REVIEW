Vim 分阶速记卡（适配 Ubuntu‑命令行 + Python 写作业）
规则约束：
优先禁用方向键，只用 hjkl 移动光标，强制养成习惯，减少按键冗余
严格按 Level0→1→2→3 顺序练习，上一级完全熟练再开启下一级
练习载体：~/python_hw 下的 py 文件，只用来写习题、调试代码
三大模式牢记：
普通模式 (Normal)：Esc，执行跳转、复制、删除、命令（基础模式）
插入模式 (Insert)：i/a/o/O，输入代码文字
底线命令模式：普通模式输入 :，执行保存、配置、查找、替换
Level 0｜生存级（1‑2 天，通关标准：可独立完成 Python 作业，不会卡死）
一、模式切换
表格
命令	作用
i	光标原地进入插入模式（最常用）
a	光标后一格进入插入模式
Esc	切回普通模式（所有命令前提）
二、保存 / 退出（必背）
plaintext
:w      仅保存，不退出
:wq     保存并退出（写完作业固定指令）
:q!     强制退出，不保存（改错、误进入界面）
三、光标移动（禁止方向键）
plaintext
h 左 j 下 k 上 l 右
四、最简编辑（普通模式）
表格
指令	功能
x	删除光标单个字符
dd	删除一整行代码
Level0 标准实操模板（以后写作业固定流程）
bash
运行
cd ~/python_hw/week01
vim hw01‑p1.py
i       # 写Python代码
Esc
:wq
python3 hw01-p1.py
达标条件：连续完成 3 次 Python 作业，全程不切回 nano。
Level 1｜效率入门级（3‑7 天，目标：实现批量操作、调试报错代码）
1. 快速光标跳转（减少连续敲击 hjkl）
表格
命令	功能
0	跳至本行开头
$	跳至本行末尾
gg	跳转文件首行
G	跳转文件末行
:15	直接跳到第 15 行（Python 报错调试核心）
2. 复制‑粘贴‑删除（整行操作）
表格
指令	作用
yy	复制一整行
y2y	复制连续 2 行
d2d	删除连续 2 行
p	粘贴到光标下一行
3. 撤销 & 重做（改错回滚）
plaintext
u        撤销上一步
Ctrl‑r   重做（恢复撤销内容）
4. Vim Python 配置（写入～/.vimrc，一次性永久生效）
打开配置文件：vim ~/.vimrc，粘贴下面内容
vim
syntax on
set number                "显示行号
set expandtab             "Tab转为4个空格，严格适配Python语法
set tabstop=4
set shiftwidth=4
set autoindent            "回车自动继承缩进，写if/for/def自动缩进
set cursorline             "高亮当前行，方便定位代码
保存退出后执行：source ~/.vimrc 立即生效。
Level1 达标任务
复制函数代码块，使用 yy+p 粘贴复用；
根据 Python 报错行号，用:行号快速跳转排错；
依靠 u / Ctrl‑r 来回撤销修改。
Level 2｜精简进阶级（1‑2 周，减少 Esc+i 来回切换，单词级操作 + 批量注释）
1. 单词级光标跳转（跳过一整个变量，不用挨个字母移动）
plaintext
w  跳到下一个单词开头
b  跳到上一个单词开头
2. 单词级删除
plaintext
dw  删除光标往后一整个单词
3. 一键新建行（省去 Esc‑回车‑i 三步，写代码提速重点）
表格
指令	功能
o	在光标下方新建一行，直接进入插入模式
O	在光标上方新建一行，直接进入插入模式
4. 行可视化模式（大写 V，批量注释 Python 代码）
普通模式按大写 V，进入行选中模式；
j/k 上下选中多行代码；
批量加 # 注释：:s/^/# /
取消注释：:s/^# //
Level2 达标任务
用 w、b 在变量名之间快速跳转；
使用 o/O 写循环、函数；
批量注释一整个函数代码段。
Level 3｜高阶熟练级（长期打磨，适配项目级代码，实现极致精简）
1. 数字 + 指令通用范式（所有命令都可复用）
格式：数字+命令
示例：
3dd：删除 3 行
4yy：复制 4 行
5w：向后跳 5 个单词
2. 块状可视化（Ctrl+v，垂直选中，批量调整缩进）
Ctrl+v，垂直框选文字，用来批量增减 Python 缩进。
3. 文本查找（定位 def、变量名）
普通模式输入 /def，检索所有 def 函数；
n：下一个匹配项
N：上一个匹配项
4. 全局变量替换（整文件一键重命名变量）
plaintext
:%s/旧变量名/新变量名/g
5. 分屏（一边写代码、一边在终端运行 Python）
plaintext
:sp    水平横向分屏
:vsp   垂直纵向分屏
Ctrl‑w + h/j/k/l 切换分屏窗口
极简每日练习计划（照做就行）
前 2 天：只练 Level0，完成 3 份作业；
第 3‑7 天：吃透 Level1，习惯行跳转、复制粘贴、行号调试；
第 8‑21 天：吃透 Level2，熟练 o/O、单词移动、批量注释；
21 天之后：再学习 Level3 高阶语法。
后续我可以再给你做一个一周打卡版，每天定 15 分钟的 Vim 训练任务，要不要？